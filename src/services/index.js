import { createService } from "../helpers/createRestService";

export const tasksService = createService("tasks");

export const teamsService = createService("teams");

export const boardsService = createService("boards");

export const projectsService = createService("projects");

export const usersService = createService("users");
